# Option 1: API key via .env (also possible: ~/.config/clockify-export/config.json or env var)
CLOCKIFY_API_KEY=your-api-key-here

# Option 2: export directory via .env (default: ./export)
# CLOCKIFY_EXPORT_DIR=/path/to/exports